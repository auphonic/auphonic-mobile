@import 'nib'

.scrollable
  display block

  overflow auto
  -webkit-overflow-scrolling touch

.scroll-content
  box-sizing border-box
  display block

  min-height 101%
  transform translate3d(0, 0, 0) // iOS cuts of elements if this is missing
