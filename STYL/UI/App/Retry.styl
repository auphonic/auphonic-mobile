@import 'nib'
@import '../../mixin/expand'

div.retry
  z-index 21
  expand()

  a.expand
    position absolute
    top 50%
    width 90%
    height 39px
    line-height 39px

    // Specificity
    .ios &, .android &
      margin -19px auto 0
