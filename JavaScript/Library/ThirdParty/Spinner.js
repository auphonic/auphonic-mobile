// Spinner is bundled with the App because it is used at load time.
// If necessary the Spinner can be replaced here but changing the one that
// is used on start will require an app store push.

module.exports = window.Spinner;
